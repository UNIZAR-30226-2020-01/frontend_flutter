import 'package:flutter/material.dart';

class GenresScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Genres Screen'),
      ),
    );
  }
}